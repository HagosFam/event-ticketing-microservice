#spring:
#  application:
#    name: eurekaServer
#  config:
#    import: optional:configserver:http://localhost:8071
management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

